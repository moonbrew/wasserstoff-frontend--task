import { Lexend } from 'next/font/google';

export const lexend_700 = Lexend({
    weight: '700',
    subsets: ['latin']
});

export const lexend_800 = Lexend({
    weight: '800',
    subsets: ['latin']
});
