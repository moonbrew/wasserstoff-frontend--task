import { Lexend } from 'next/font/google';

export const lexend_bold = Lexend({
    weight: '700',
    subsets: ['latin']
});

export const lexend_extrabold = Lexend({
    weight: '800',
    subsets: ['latin']
});
